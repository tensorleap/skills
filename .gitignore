# Python
__pycache__/
*.py[cod]
.venv/
venv/

# OS / editor
.DS_Store
*.swp
.idea/

# Installer / build scratch
*.tmp
/tmp-*/

# Internal build brief / session handoff — never publish (contains internal refs)
PLAN.md

# skill-eval harness — regenerated / machine-local, never committed
eval/.fixtures/
eval/reports/
eval/runtime_prerequisites.local.json
eval/.shim/
